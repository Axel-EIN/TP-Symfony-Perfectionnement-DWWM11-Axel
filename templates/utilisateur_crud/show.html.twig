{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
    <h1>Utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ utilisateur.pseudo }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ utilisateur.roles ? utilisateur.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>MotDePasse</th>
                <td>{{ utilisateur.motDePasse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('utilisateur_crud_index') }}">back to list</a>

    <a href="{{ path('utilisateur_crud_edit', {'id': utilisateur.id}) }}">edit</a>

    {{ include('utilisateur_crud/_delete_form.html.twig') }}
{% endblock %}
